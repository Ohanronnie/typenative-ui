import { usizeToU64 } from "std/alloc";
import { Array } from "std/collections";
import { String } from "std/string";
import { ButtonProps, CanvasProps, DialogProps, EmptyProps, GridProps, ImageProps, ListProps, MenuProps, ProgressProps, ScrollViewProps, SliderProps, SplitViewProps, StackProps, Style, styleSnapshot, SwitchProps, TextInputProps, TextProps, ToolbarProps, ViewProps, WindowProps } from "./core/types";
import { AccessibilityRole, Element, ElementRef, FlexDirection, StyleSnapshot, bindElementRef, buttonTypeId, canvasTypeId, createHost, createHostAction, createHostActionWithStyle, createHostWithStyle, createText, createTextInputWithSelection, createFragment, defaultStyleSnapshot, dialogTypeId, gridTypeId, imageTypeId, keyInteger, keyNone, kindButton, kindCanvas, kindDialog, kindGrid, kindImage, kindList, kindMenu, kindProgress, kindScrollView, kindSlider, kindSplitView, kindStack, kindSwitch, kindText, kindToolbar, kindView, kindWindow, lengthKind, lengthValue, listTypeId, menuTypeId, progressTypeId, scrollViewTypeId, sliderTypeId, splitViewTypeId, stackTypeId, switchTypeId, textTypeId, toolbarTypeId, viewTypeId, windowTypeId } from "./tnx-runtime";

function oneChild(child: Element): Array<Element> {
  const children = new Array<Element>({
    capacity: 1usize
  });
  children.push(move child);
  return move children;
}
function noChildren(): Array<Element> {
  return new Array<Element>({
    capacity: 0usize
  });
}
function withReference(element: Element, reference: &ElementRef | undefined): Element {
  if (reference !== undefined) {
    return bindElementRef(move element, reference!);
  }
  return move element;
}
function applyMetadata(element: &mut Element, label: string | undefined, role: AccessibilityRole | undefined, enabled: bool | undefined): void {
  if (label !== undefined) {
    element.setAccessibilityLabel(label!.clone());
  }
  if (role !== undefined) {
    element.setAccessibilityRole(role!);
  }
  if (enabled !== undefined) {
    element.setAccessibilityEnabled(enabled!);
  }
}
function finishElement(element: Element, reference: &ElementRef | undefined, label: string | undefined, role: AccessibilityRole | undefined, enabled: bool | undefined): Element {
  let result = move element;
  applyMetadata( &mut result, label, role, enabled);
  return withReference(move result, reference);
}
function withStyleAndMetadata(kind: i32, typeId: u64, children: Array<Element>, style: StyleSnapshot, hasStyle: bool, reference: &ElementRef | undefined, label: string | undefined, role: AccessibilityRole | undefined, enabled: bool | undefined): Element {
  let element = createHost(kind, typeId, keyNone(), String(""), move children);
  if (hasStyle) {
    element.setStyle(move style);
  }
  applyMetadata( &mut element, label, role, enabled);
  return withReference(move element, reference);
}
export function View(props: ViewProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  let snapshotValue = defaultStyleSnapshot();
  let hasStyle = false;
  if (props.style !== undefined) {
    snapshotValue = styleSnapshot( &props.style!);
    hasStyle = true;
  }
  const children = move props.children;
  return withStyleAndMetadata(kindView(), viewTypeId(), oneChild(move children), move snapshotValue, hasStyle, reference, label, role, enabled);
}
export function Text(props: TextProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  const value = props.children.clone();
  let element = createHost(kindText(), textTypeId(), keyNone(), move value, noChildren());
  applyMetadata( &mut element, label, role, enabled);
  return withReference(move element, reference);
}
export function Button(props: ButtonProps): Element {
  const reference = props.elementRef;
  const labelMetadata = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const accessibilityEnabled = props.accessibilityEnabled;
  const enabled = props.enabled;
  const label = props.children.clone();
  const onPress = props.onPress;
  const action = move(): void => {
    onPress();
  };
  let element = createHostAction(kindButton(), buttonTypeId(), keyNone(), move label, noChildren(), move action);
  applyMetadata( &mut element, labelMetadata, role, accessibilityEnabled);
  if (enabled !== undefined) {
    element.setEnabled(enabled!);
  }
  return withReference(move element, reference);
}
export function TextInput(props: TextInputProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const accessibilityEnabled = props.accessibilityEnabled;
  const enabled = props.enabled;
  const selection = props.selection;
  const onChangeText = props.onChangeText;
  const onSubmitEditing = props.onSubmitEditing;
  const onFocus = props.onFocus;
  const onBlur = props.onBlur;
  const editable = props.editable;
  const autoFocus = props.autoFocus;
  const multiline = props.multiline;
  const keyboardType = props.keyboardType;
  const valueProp = props.value;
  const defaultValueProp = props.defaultValue;
  const placeholderProp = props.placeholder;
  const hasValue = valueProp !== undefined;
  const hasDefaultValue = defaultValueProp !== undefined;
  let value = String("");
  if (hasValue) {
    value = move valueProp!;
  } else if (hasDefaultValue) {
    value = move defaultValueProp!;
  }
  let placeholder = String("");
  if (placeholderProp !== undefined) {
    placeholder = move placeholderProp!;
  }
  let change: (string) => void = move(_: string): void => {
  };
  if (onChangeText !== undefined) {
    change = onChangeText!;
  }
  let element = createTextInputWithSelection(move value, move placeholder, move change, undefined, undefined);
  if (hasValue) {
    element.setControlled(true);
  }
  if (selection !== undefined) {
    element.setSelection(selection!.start, selection!.end);
  }
  if (onSubmitEditing !== undefined) {
    element.setSubmit(onSubmitEditing!);
  }
  if (onFocus !== undefined) {
    element.setFocus(onFocus!);
  }
  if (onBlur !== undefined) {
    element.setBlur(onBlur!);
  }
  if (editable !== undefined) {
    element.setEditable(editable!);
  }
  if (autoFocus !== undefined) {
    element.setAutoFocus(autoFocus!);
  }
  if (multiline !== undefined) {
    element.setMultiline(multiline!);
  }
  if (keyboardType !== undefined) {
    element.setKeyboardType(keyboardType!);
  }
  applyMetadata( &mut element, label, role, accessibilityEnabled);
  if (enabled !== undefined) {
    element.setEnabled(enabled!);
  }
  return withReference(move element, reference);
}
export function Window(props: WindowProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  const title = props.title.clone();
  const width = props.width;
  const height = props.height;
  const onClose = props.onClose;
  const onResize = props.onResize;
  const closePolicy = props.closePolicy;
  let element = Element.empty();
  if (width !== undefined || height !== undefined) {
    const style = new Style();
    if (width !== undefined) {
      style.width = width!;
    }
    if (height !== undefined) {
      style.height = height!;
    }
    const child = move props.children;
    element = createHostWithStyle(kindWindow(), windowTypeId(), keyNone(), move title, oneChild(move child), styleSnapshot( &style));
  } else {
    const child = move props.children;
    element = createHost(kindWindow(), windowTypeId(), keyNone(), move title, oneChild(move child));
  }
  if (onClose !== undefined) {
    element.setCloseAction(onClose!);
  }
  if (onResize !== undefined) {
    element.setResizeAction(onResize!);
  }
  if (closePolicy !== undefined) {
    element.setClosePolicy(closePolicy!);
  }
  return finishElement(move element, reference, label, role, enabled);
}
export function Image(props: ImageProps): Element {
  const reference = props.elementRef;
  const metadataLabel = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  const source = props.source.clone();
  let label = String("");
  if (props.alt !== undefined) {
    label = props.alt!.clone();
  }
  let element = createHost(kindImage(), imageTypeId(), keyNone(), move label, noChildren());
  element.setImageSource(move source);
  if (props.style !== undefined) {
    element.setStyle(styleSnapshot( &props.style!));
  }
  applyMetadata( &mut element, metadataLabel, role, enabled);
  return withReference(move element, reference);
}
export function ScrollView(props: ScrollViewProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  let style = defaultStyleSnapshot();
  let hasStyle = false;
  if (props.style !== undefined) {
    style = styleSnapshot( &props.style!);
    hasStyle = true;
  }
  const children = move props.children;
  return withStyleAndMetadata(kindScrollView(), scrollViewTypeId(), oneChild(move children), move style, hasStyle, reference, label, role, enabled);
}
export function List(props: ListProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  let style = defaultStyleSnapshot();
  let hasStyle = false;
  if (props.style !== undefined) {
    style = styleSnapshot( &props.style!);
    hasStyle = true;
  }
  let itemHeight = 0.0f32;
  if (props.itemHeight !== undefined && lengthKind( &props.itemHeight!) === 1i32) {
    itemHeight = lengthValue( &props.itemHeight!);
  }
  let firstIndex = 0usize;
  if (props.firstVisibleIndex !== undefined) {
    firstIndex = props.firstVisibleIndex!;
  }
  if (firstIndex > props.itemCount) {
    firstIndex = props.itemCount;
  }
  let visibleCount = props.itemCount;
  if (props.visibleItemCount !== undefined && props.visibleItemCount! > 0usize) {
    visibleCount = props.visibleItemCount!;
  }
  let overscan = 2usize;
  if (props.overscan !== undefined) {
    overscan = props.overscan!;
  }
  let end = firstIndex + visibleCount + overscan;
  if (end > props.itemCount) {
    end = props.itemCount;
  }
  const children = new Array<Element>({
    capacity: end - firstIndex
  });
  const renderItem = props.renderItem;
  let index = firstIndex;
  while (index<end) {
    let child = renderItem(index);
    child.setKeyIfAbsent(keyInteger(usizeToU64(index)));
    children.push(move child);
    index = index + 1usize;
  }
  let element = withStyleAndMetadata(kindList(), listTypeId(), move children, move style, hasStyle, reference, label, role, enabled);
  element.setVirtualList(props.itemCount, firstIndex, itemHeight);
  if (props.onVisibleRangeChange !== undefined) {
    element.setVisibleRangeChange(props.onVisibleRangeChange!);
  }
  return move element;
}
export function Switch(props: SwitchProps): Element {
  const reference = props.elementRef;
  const labelMetadata = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const accessibilityEnabled = props.accessibilityEnabled;
  const enabled = props.enabled;
  const onChange = props.onChange;
  const valueProp = props.value;
  const defaultValue = props.defaultValue;
  let value = false;
  if (valueProp !== undefined) {
    value = valueProp!;
  } else if (defaultValue !== undefined) {
    value = defaultValue!;
  }
  let element = createHost(kindSwitch(), switchTypeId(), keyNone(), String(""), noChildren());
  element.setChecked(value);
  if (onChange !== undefined) {
    element.setBooleanChange(onChange!);
  }
  applyMetadata( &mut element, labelMetadata, role, accessibilityEnabled);
  if (enabled !== undefined) {
    element.setEnabled(enabled!);
  }
  return withReference(move element, reference);
}
export function Slider(props: SliderProps): Element {
  const reference = props.elementRef;
  const labelMetadata = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const accessibilityEnabled = props.accessibilityEnabled;
  const enabled = props.enabled;
  const onChange = props.onChange;
  const valueProp = props.value;
  const defaultValue = props.defaultValue;
  const minimumProp = props.minimumValue;
  const maximumProp = props.maximumValue;
  let value = 0.0f32;
  if (valueProp !== undefined) {
    value = valueProp!;
  } else if (defaultValue !== undefined) {
    value = defaultValue!;
  }
  let minimum = 0.0f32;
  if (minimumProp !== undefined) {
    minimum = minimumProp!;
  }
  let maximum = 1.0f32;
  if (maximumProp !== undefined) {
    maximum = maximumProp!;
  }
  let element = createHost(kindSlider(), sliderTypeId(), keyNone(), String(""), noChildren());
  element.setNumericValue(value);
  element.setValueRange(minimum, maximum);
  if (onChange !== undefined) {
    element.setNumericChange(onChange!);
  }
  applyMetadata( &mut element, labelMetadata, role, accessibilityEnabled);
  if (enabled !== undefined) {
    element.setEnabled(enabled!);
  }
  return withReference(move element, reference);
}
export function Progress(props: ProgressProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  const value = props.value;
  const minimumProp = props.minimumValue;
  const maximumProp = props.maximumValue;
  let minimum = 0.0f32;
  if (minimumProp !== undefined) {
    minimum = minimumProp!;
  }
  let maximum = 1.0f32;
  if (maximumProp !== undefined) {
    maximum = maximumProp!;
  }
  const styleProp = props.style;
  let element = createHost(kindProgress(), progressTypeId(), keyNone(), String(""), noChildren());
  if (styleProp !== undefined) {
    element.setStyle(styleSnapshot( &styleProp!));
  }
  element.setNumericValue(value);
  element.setValueRange(minimum, maximum);
  applyMetadata( &mut element, label, role, enabled);
  return withReference(move element, reference);
}
export function Menu(props: MenuProps): Element {
  const reference = props.elementRef;
  const labelMetadata = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  const titleProp = props.title;
  const childProp = props.children;
  let title = String("");
  if (titleProp !== undefined) {
    title = titleProp!.clone();
  }
  let children = noChildren();
  if (childProp !== undefined) {
    children = oneChild(move childProp!);
  }
  let element = createHost(kindMenu(), menuTypeId(), keyNone(), move title, move children);
  applyMetadata( &mut element, labelMetadata, role, enabled);
  return withReference(move element, reference);
}
export function Dialog(props: DialogProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  const title = props.title.clone();
  const child = move props.children;
  let element = createHost(kindDialog(), dialogTypeId(), keyNone(), move title, oneChild(move child));
  applyMetadata( &mut element, label, role, enabled);
  return withReference(move element, reference);
}
export function Toolbar(props: ToolbarProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  let style = defaultStyleSnapshot();
  let hasStyle = false;
  if (props.style !== undefined) {
    style = styleSnapshot( &props.style!);
    hasStyle = true;
  }
  const child = move props.children;
  return withStyleAndMetadata(kindToolbar(), toolbarTypeId(), oneChild(move child), move style, hasStyle, reference, label, role, enabled);
}
export function SplitView(props: SplitViewProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  let style = defaultStyleSnapshot();
  let hasStyle = false;
  if (props.style !== undefined) {
    style = styleSnapshot( &props.style!);
    hasStyle = true;
  }
  const child = move props.children;
  return withStyleAndMetadata(kindSplitView(), splitViewTypeId(), oneChild(move child), move style, hasStyle, reference, label, role, enabled);
}
export function Stack(props: StackProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  let style = defaultStyleSnapshot();
  let hasStyle = false;
  if (props.style !== undefined) {
    style = styleSnapshot( &props.style!);
    hasStyle = true;
  }
  const child = move props.children;
  return withStyleAndMetadata(kindStack(), stackTypeId(), oneChild(move child), move style, hasStyle, reference, label, role, enabled);
}
export function Grid(props: GridProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  let style = defaultStyleSnapshot();
  let hasStyle = false;
  if (props.style !== undefined) {
    style = styleSnapshot( &props.style!);
    hasStyle = true;
  }
  const child = move props.children;
  return withStyleAndMetadata(kindGrid(), gridTypeId(), oneChild(move child), move style, hasStyle, reference, label, role, enabled);
}
export function Canvas(props: CanvasProps): Element {
  const reference = props.elementRef;
  const label = props.accessibilityLabel;
  const role = props.accessibilityRole;
  const enabled = props.accessibilityEnabled;
  let style = defaultStyleSnapshot();
  let hasStyle = false;
  if (props.style !== undefined) {
    style = styleSnapshot( &props.style!);
    hasStyle = true;
  }
  const childProp = props.children;
  let children = noChildren();
  if (childProp !== undefined) {
    children = oneChild(move childProp!);
  }
  return withStyleAndMetadata(kindCanvas(), canvasTypeId(), move children, move style, hasStyle, reference, label, role, enabled);
}
export function emptyView(props: EmptyProps): Element {
  return createHost(kindView(), viewTypeId(), keyNone(), String(""), noChildren());
}
export function createTextNode(value: &str): Element {
  return createText(value);
}
export function styledView(style: &Style, child: Element): Element {
  return createHostWithStyle(kindView(), viewTypeId(), keyNone(), String(""), oneChild(move child), styleSnapshot(style));
}
export function stack(style: &Style, children: Array<Element >): Element {
  return createHostWithStyle(kindView(), viewTypeId(), keyNone(), String(""), move children, styleSnapshot(style));
}
export function createFragmentChildren(children: Array<Element >): Element {
  return createFragment(move children);
}
export function row(children: Array<Element >): Element {
  const style = new Style();
  style.flexDirection = FlexDirection.Row;
  return stack( &style, move children);
}
export function column(children: Array<Element >): Element {
  const style = new Style();
  style.flexDirection = FlexDirection.Column;
  return stack( &style, move children);
}
export function button(label: &str, action: () => void): Element {
  return createHostAction(kindButton(), buttonTypeId(), keyNone(), String(label), noChildren(), move action);
}
export function styledButton(style: &Style, label: &str, action: () => void): Element {
  return createHostActionWithStyle(kindButton(), buttonTypeId(), keyNone(), String(label), noChildren(), move action, styleSnapshot(style));
}
export function window(title: &str, child: Element): Element {
  return createHost(kindWindow(), windowTypeId(), keyNone(), String(title), oneChild(move child));
}
